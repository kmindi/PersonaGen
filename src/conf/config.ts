// JSON config for this frontend project
export const config = {

    // identifier used for localStorage
    appId: "personaGen",

    // provided languages
    languages: ["en", "de"],

    // build Date
    buildDate: "$BUILDDATE",
    // version
    version: "$VERSION"
};
