// JSON config for this frontend project
export const config = {

    // identifier used for localStorage
    appId: "personaGen",

    // maximum number of personas generated
    maxNumberOfPersonas: 100,

    // provided languages
    languages: ["en", "de"],

    // build Date
    buildDate: "$BUILDDATE",
    // version
    version: "$VERSION"
};
